# Generated by ci.workflows; edit the canonical source, not this file.
# Source: .github/workflow-sources/schedules/release/nightly.yaml
# Directory guide: .github/workflow-sources/schedules/release/README.md
# Source SHA256: ea32c6d6dd166dd3ece2ce906098ee93784936c55b2e466c15e0cf29be9f9a32
# Regenerate: python -m ci.workflows --write

name: Schedule nightly release preparation

on:
  schedule:
    - cron: '0 3 * * *'

# Preserve the token scopes already declared by the called jobs.
permissions:
  actions: read
  contents: read
  id-token: write
  packages: write

jobs:
  run:
    uses: ./.github/workflows/release-nightly.yaml
    secrets: inherit
