# Generated by ci.workflows; edit the canonical source, not this file.
# Source: ci/workflows/schedules/release/nightly.yaml
# Source SHA256: fd1d0df2e0afbc6817e7c6b26d7ab018417109465603042ffb17aab36440e094
# Regenerate: python -m ci.workflows --write

name: Schedule release-nightly

on:
  schedule:
    - cron: '0 3 * * *'

# Preserve the token scopes already declared by the called jobs.
permissions:
  actions: read
  contents: read
  id-token: write
  packages: write

jobs:
  run:
    uses: ./.github/workflows/release-nightly.yaml
    secrets: inherit
