# Generated by ci.workflows; edit the canonical source, not this file.
# Source: .github/workflow-sources/schedules/repository/runner-monitor.yml
# Directory guide: .github/workflow-sources/schedules/repository/README.md
# Source SHA256: ecb5829e45a4b22e1eaaab321b42935c2de8db7c7643643d27d4b94b00cd48de
# Regenerate: python -m ci.workflows --write

name: Schedule CI worker monitoring

on:
  schedule:
    - cron: '0 0 * * *'

# Preserve the token scopes already declared by the called jobs.
permissions:
  actions: read
  contents: read

jobs:
  run:
    uses: ./.github/workflows/repository-runner-monitor.yml
    secrets: inherit
