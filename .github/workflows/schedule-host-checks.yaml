# Generated by ci.workflows; edit the canonical source, not this file.
# Source: ci/workflows/schedules/host/checks.yaml
# Source SHA256: 996925c24a247a00a3dfefaea0fb056caac703fe32d8ac1fe250df96030547b1
# Regenerate: python -m ci.workflows --write

name: Schedule host-checks

on:
  schedule:
    - cron: '30 2 * * *'

# Preserve the token scopes already declared by the called jobs.
permissions:
  contents: read

jobs:
  run:
    uses: ./.github/workflows/host-checks.yaml
    secrets: inherit
