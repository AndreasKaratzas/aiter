# Generated by ci.workflows; edit the canonical source, not this file.
# Source: ci/workflows/schedules/product/legacy.yaml
# Source SHA256: 3a15a822cb5d3e62ff97345917b1fdb4d2ca5dd5a30f6e51ce50385864e41eee
# Regenerate: python -m ci.workflows --write

name: Schedule product-legacy

on:
  schedule:
    - cron: '0 22 * * *'

# Preserve the token scopes already declared by the called jobs.
permissions:
  contents: read
  id-token: write
  pull-requests: read

jobs:
  run:
    uses: ./.github/workflows/product-legacy.yaml
    secrets: inherit
