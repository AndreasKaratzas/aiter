# Generated by ci.workflows; edit the canonical source, not this file.
# Source: .github/workflow-sources/schedules/frameworks/vllm/benchmarks-daily.yaml
# Directory guide: .github/workflow-sources/schedules/frameworks/vllm/README.md
# Source SHA256: 3033b432fe06920eb893f25f5239b3cf28330649539e3e0d333c0c4a8d209095
# Regenerate: python -m ci.workflows --write

name: Schedule vLLM daily model benchmarks

on:
  schedule:
    - cron: '45 19 * * *'

# Preserve the token scopes already declared by the called jobs.
permissions:
  actions: read
  contents: read

jobs:
  run:
    uses: ./.github/workflows/frameworks-vllm-model-benchmarks.yaml
    with:
      benchmark_profile: 'smoke'
      gpus: '0'
      benchmark_cases: ''
    secrets: inherit
