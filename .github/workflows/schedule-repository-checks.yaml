# Generated by ci.workflows; edit the canonical source, not this file.
# Source: .github/workflow-sources/schedules/repository/checks.yaml
# Directory guide: .github/workflow-sources/schedules/repository/README.md
# Source SHA256: 1999670ca2c33068a9f75ccc972a0b2d1d64399d8d2a5b2c003f0d4d13e843bd
# Regenerate: python -m ci.workflows --write

name: Schedule repository checks

on:
  schedule:
    - cron: '30 2 * * *'

# Preserve the token scopes already declared by the called jobs.
permissions:
  contents: read

jobs:
  run:
    uses: ./.github/workflows/repository-checks.yaml
    secrets: inherit
