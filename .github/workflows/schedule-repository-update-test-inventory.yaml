# Generated by ci.workflows; edit the canonical source, not this file.
# Source: .github/workflow-sources/schedules/repository/update-test-inventory.yaml
# Directory guide: .github/workflow-sources/schedules/repository/README.md
# Source SHA256: 2f25ff780a90cf95e52cbd061dad968981676d046df18ca0f4486a4c80a175c0
# Regenerate: python -m ci.workflows --write

name: Schedule test timing updates

on:
  schedule:
    - cron: '0 3 * * 1'

# Preserve the token scopes already declared by the called jobs.
permissions:
  actions: read
  contents: write
  pull-requests: write

jobs:
  run:
    uses: ./.github/workflows/repository-update-test-inventory.yaml
    secrets: inherit
