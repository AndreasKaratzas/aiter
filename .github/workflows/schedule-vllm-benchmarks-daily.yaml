# Generated by ci.workflows; edit the canonical source, not this file.
# Source: ci/workflows/schedules/clients/vllm/benchmarks-daily.yaml
# Source SHA256: 37d28d716a6d71614999e5b0e2e828cd4d8c0fd22cf781a262e4b559bcbb09cf
# Regenerate: python -m ci.workflows --write

name: Schedule client-vllm-model-benchmarks daily

on:
  schedule:
    - cron: '45 19 * * *'

# Preserve the token scopes already declared by the called jobs.
permissions:
  actions: read
  contents: read

jobs:
  run:
    uses: ./.github/workflows/client-vllm-model-benchmarks.yaml
    with:
      benchmark_profile: 'smoke'
      gpus: '0'
      benchmark_cases: ''
    secrets: inherit
