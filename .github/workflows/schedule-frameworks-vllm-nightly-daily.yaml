# Generated by ci.workflows; edit the canonical source, not this file.
# Source: .github/workflow-sources/schedules/frameworks/vllm/nightly-daily.yaml
# Directory guide: .github/workflow-sources/schedules/frameworks/vllm/README.md
# Source SHA256: 0617ea2248b91fcdf5251b973dac08507f3b00dbdda07c18a55e241f84501a3a
# Regenerate: python -m ci.workflows --write

name: Schedule vLLM daily tests

on:
  schedule:
    - cron: '45 17 * * *'

# Preserve the token scopes already declared by the called jobs.
permissions:
  actions: read
  contents: read

jobs:
  run:
    uses: ./.github/workflows/frameworks-vllm-nightly.yaml
    with:
      workload_profile: 'vllm-nightly'
    secrets: inherit
