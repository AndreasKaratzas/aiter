# Generated by ci.workflows; edit the canonical source, not this file.
# Source: .github/workflow-sources/schedules/library/legacy.yaml
# Directory guide: .github/workflow-sources/schedules/library/README.md
# Source SHA256: bc726bac0a0638572d6f8791890b77c236d55d40c8880bf54d7efdcc46ad59ed
# Regenerate: python -m ci.workflows --write

name: Schedule AITER operator tests

on:
  schedule:
    - cron: '0 22 * * *'

# Preserve the token scopes already declared by the called jobs.
permissions:
  contents: read
  id-token: write
  pull-requests: read

jobs:
  run:
    uses: ./.github/workflows/library-legacy.yaml
    secrets: inherit
