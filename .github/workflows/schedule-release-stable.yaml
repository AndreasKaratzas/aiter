# Generated by ci.workflows; edit the canonical source, not this file.
# Source: .github/workflow-sources/schedules/release/stable.yaml
# Directory guide: .github/workflow-sources/schedules/release/README.md
# Source SHA256: e20102983157607900b4c9a211ea332e55331def604fb00af5600cf2eb84e489
# Regenerate: python -m ci.workflows --write

name: Schedule stable release preparation

on:
  schedule:
    - cron: '0 6 * * 1'

# Preserve the token scopes already declared by the called jobs.
permissions:
  actions: read
  contents: write
  id-token: write
  packages: write

jobs:
  run:
    uses: ./.github/workflows/release-stable.yaml
    secrets: inherit
