# Generated by ci.workflows; edit the canonical source, not this file.
# Source: ci/workflows/schedules/release/stable.yaml
# Source SHA256: 0939d4140bee2ddb8c38a8a2532d4277edfd274f0f5b50e13ab482bfba60cf9a
# Regenerate: python -m ci.workflows --write

name: Schedule release-stable

on:
  schedule:
    - cron: '0 6 * * 1'

# Preserve the token scopes already declared by the called jobs.
permissions:
  actions: read
  contents: write
  id-token: write
  packages: write

jobs:
  run:
    uses: ./.github/workflows/release-stable.yaml
    secrets: inherit
