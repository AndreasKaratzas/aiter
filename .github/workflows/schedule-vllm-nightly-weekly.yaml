# Generated by ci.workflows; edit the canonical source, not this file.
# Source: ci/workflows/schedules/clients/vllm/nightly-weekly.yaml
# Source SHA256: 00686176c908122294419fbd94608e2a12b915717e4ed09147e99fee2e0252a9
# Regenerate: python -m ci.workflows --write

name: Schedule client-vllm-nightly weekly

on:
  schedule:
    - cron: '15 20 * * 0'

# Preserve the token scopes already declared by the called jobs.
permissions:
  actions: read
  contents: read

jobs:
  run:
    uses: ./.github/workflows/client-vllm-nightly.yaml
    with:
      workload_profile: 'vllm-extended'
    secrets: inherit
