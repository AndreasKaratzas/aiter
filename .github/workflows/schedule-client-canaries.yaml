# Generated by ci.workflows; edit the canonical source, not this file.
# Source: ci/workflows/schedules/clients/common/canaries.yaml
# Source SHA256: 95c2bd57dfcaf9c53f7bd8c4b7c46c2ad3dc0e086a936fd88d43545e6728d8a4
# Regenerate: python -m ci.workflows --write

name: Schedule client-canaries

on:
  schedule:
    - cron: '15 17 * * *'

# Preserve the token scopes already declared by the called jobs.
permissions:
  actions: read
  contents: read

jobs:
  run:
    uses: ./.github/workflows/client-canaries.yaml
    secrets: inherit
