# Generated by ci.workflows; edit the canonical source, not this file.
# Source: .github/workflow-sources/schedules/frameworks/atom/disaggregation.yaml
# Directory guide: .github/workflow-sources/schedules/frameworks/atom/README.md
# Source SHA256: 03b4033f1db20b5e41b5c714f65086fa3f6e80911646872f4233ee8eb4232b61
# Regenerate: python -m ci.workflows --write

name: Schedule ATOM disaggregated serving

on:
  schedule:
    - cron: '0 16 * * *'

# Preserve the token scopes already declared by the called jobs.
permissions:
  contents: read

jobs:
  run:
    uses: ./.github/workflows/frameworks-atom-disaggregation.yaml
    secrets: inherit
