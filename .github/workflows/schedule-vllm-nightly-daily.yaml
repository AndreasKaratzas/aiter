# Generated by ci.workflows; edit the canonical source, not this file.
# Source: ci/workflows/schedules/clients/vllm/nightly-daily.yaml
# Source SHA256: 11bdb6af67ba8e7fc1c2283eaa228a43528abab5548952bf2d45da22d890acee
# Regenerate: python -m ci.workflows --write

name: Schedule client-vllm-nightly daily

on:
  schedule:
    - cron: '45 17 * * *'

# Preserve the token scopes already declared by the called jobs.
permissions:
  actions: read
  contents: read

jobs:
  run:
    uses: ./.github/workflows/client-vllm-nightly.yaml
    with:
      workload_profile: 'vllm-nightly'
    secrets: inherit
