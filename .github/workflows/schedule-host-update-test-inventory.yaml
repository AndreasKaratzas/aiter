# Generated by ci.workflows; edit the canonical source, not this file.
# Source: ci/workflows/schedules/host/update-test-inventory.yaml
# Source SHA256: 06fc645bdfcb152823912f908ed33d5b2bee32cd3e37cf05d7f08b21ae0b360c
# Regenerate: python -m ci.workflows --write

name: Schedule host-update-test-inventory

on:
  schedule:
    - cron: '0 3 * * 1'

# Preserve the token scopes already declared by the called jobs.
permissions:
  actions: read
  contents: write
  pull-requests: write

jobs:
  run:
    uses: ./.github/workflows/host-update-test-inventory.yaml
    secrets: inherit
