# Generated by ci.workflows; edit the canonical source, not this file.
# Source: .github/workflow-sources/schedules/frameworks/vllm/nightly-weekly.yaml
# Directory guide: .github/workflow-sources/schedules/frameworks/vllm/README.md
# Source SHA256: 587d90fa6442029a438f8a95d414eb45aa71b9c942e619f328476f24e9e94689
# Regenerate: python -m ci.workflows --write

name: Schedule vLLM weekly tests

on:
  schedule:
    - cron: '15 20 * * 0'

# Preserve the token scopes already declared by the called jobs.
permissions:
  actions: read
  contents: read

jobs:
  run:
    uses: ./.github/workflows/frameworks-vllm-nightly.yaml
    with:
      workload_profile: 'vllm-extended'
    secrets: inherit
