# Generated by ci.workflows; edit the canonical source, not this file.
# Source: ci/workflows/schedules/clients/vllm/disaggregation.yaml
# Source SHA256: 0784eb4f871ebdd9047ff468828460648e6fa978ff6247182488a470f0b19be6
# Regenerate: python -m ci.workflows --write

name: Schedule client-vllm-disaggregation

on:
  schedule:
    - cron: '30 18 * * *'

# Preserve the token scopes already declared by the called jobs.
permissions:
  contents: read

jobs:
  run:
    uses: ./.github/workflows/client-vllm-disaggregation.yaml
    secrets: inherit
