# Generated by ci.workflows; edit the canonical source, not this file.
# Source: .github/workflow-sources/schedules/frameworks/vllm/disaggregation.yaml
# Directory guide: .github/workflow-sources/schedules/frameworks/vllm/README.md
# Source SHA256: d33ff172d532ff1fdbf61159f7998d63e8fca34177e807906fad747bc3ae97c4
# Regenerate: python -m ci.workflows --write

name: Schedule vLLM disaggregated serving

on:
  schedule:
    - cron: '30 18 * * *'

# Preserve the token scopes already declared by the called jobs.
permissions:
  contents: read

jobs:
  run:
    uses: ./.github/workflows/frameworks-vllm-disaggregation.yaml
    secrets: inherit
