# Generated by ci.workflows; edit the canonical source, not this file.
# Source: ci/workflows/clients/vllm/model-benchmarks.yaml
# Source SHA256: 4530b0b3a860255bb72e4ca09e546c901f4cdfd2b43e4ee0bc09fe78b0001be3
# Regenerate: python -m ci.workflows --write

name: Measure a pinned model on the current ROCm vLLM nightly

on:
  workflow_dispatch:
    inputs:
      benchmark_profile:
        description: Reviewed model measurement profile
        type: choice
        options: [baseline, smoke, throughput, topology, extended]
        default: baseline
      benchmark_cases:
        description: Optional comma-separated case IDs from the selected profile (empty selects all)
        type: string
        default: ''
      gpus:
        description: Physical GPU indices; topology and full extended profiles require two (e.g. 0,1)
        type: string
        default: '0'

  workflow_call:
    inputs:
      benchmark_profile:
        type: string
        default: baseline
      benchmark_cases:
        type: string
        default: ''
      gpus:
        type: string
        default: '0'

permissions:
  contents: read
  actions: read

# Rolling installation evidence is advisory and never advances a supported channel.
jobs:
  build:
    uses: ./.github/workflows/release-build-wheels.yaml
    with:
      release_type: none
      docker_image_py310: ${{ vars.AITER_VLLM_NIGHTLY_EXECUTOR }}
      docker_image_py312: ${{ vars.AITER_VLLM_NIGHTLY_EXECUTOR }}
      build_py310: false
      build_py312: true
      gpu_archs: gfx950
      runner: build-only-aiter
      add_date_stamp: true

  benchmark:
    needs: build
    uses: ./.github/workflows/product-run-profile.yaml
    with:
      pipeline: vllm-benchmark
      profile: ${{ inputs.benchmark_profile || 'baseline' }}
      image: ${{ vars.AITER_VLLM_NIGHTLY_EXECUTOR }}
      artifact_pattern: aiter-whl-packages-py3.12*
      gpus: ${{ inputs.gpus || '0' }}
      timeout_minutes: 120
      label: vllm-real-model-measurement
      benchmark_cases: ${{ inputs.benchmark_cases }}
