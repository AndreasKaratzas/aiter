# Generated by ci.workflows; edit the canonical source, not this file.
# Source: ci/workflows/schedules/clients/atom/disaggregation.yaml
# Source SHA256: 259f46eeb5bfe4450158835a5da8460756c736a1f3267b29f08cc99dc027fc58
# Regenerate: python -m ci.workflows --write

name: Schedule client-atom-disaggregation

on:
  schedule:
    - cron: '0 16 * * *'

# Preserve the token scopes already declared by the called jobs.
permissions:
  contents: read

jobs:
  run:
    uses: ./.github/workflows/client-atom-disaggregation.yaml
    secrets: inherit
