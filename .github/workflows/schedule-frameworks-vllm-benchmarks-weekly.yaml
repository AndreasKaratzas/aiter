# Generated by ci.workflows; edit the canonical source, not this file.
# Source: .github/workflow-sources/schedules/frameworks/vllm/benchmarks-weekly.yaml
# Directory guide: .github/workflow-sources/schedules/frameworks/vllm/README.md
# Source SHA256: 98e96dbb993839ba99f19dd18330cd4802bfea4c8d49f43cce62f254681be2fe
# Regenerate: python -m ci.workflows --write

name: Schedule vLLM weekly model benchmarks

on:
  schedule:
    - cron: '15 22 * * 0'

# Preserve the token scopes already declared by the called jobs.
permissions:
  actions: read
  contents: read

jobs:
  run:
    uses: ./.github/workflows/frameworks-vllm-model-benchmarks.yaml
    with:
      benchmark_profile: 'extended'
      gpus: '0,1'
      benchmark_cases: ''
    secrets: inherit
