# Generated by ci.workflows; edit the canonical source, not this file.
# Source: .github/workflow-sources/schedules/library/qualification.yaml
# Directory guide: .github/workflow-sources/schedules/library/README.md
# Source SHA256: 405288d9e6352d552fbf9dd0b907ee091e78be64f7acebb4fcbd085b5f77945d
# Regenerate: python -m ci.workflows --write

name: Schedule AITER and framework qualification

on:
  schedule:
    - cron: '15 6 * * 6'

# Preserve the token scopes already declared by the called jobs.
permissions:
  actions: read
  contents: read

jobs:
  run:
    uses: ./.github/workflows/library-qualification.yaml
    with:
      product_profile: 'product-extended'
    secrets: inherit
