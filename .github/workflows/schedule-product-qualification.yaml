# Generated by ci.workflows; edit the canonical source, not this file.
# Source: ci/workflows/schedules/product/qualification.yaml
# Source SHA256: 04dd21725dc47f8d52c8802aad3bc600a42233fa4239f737b8f783cd1ef9a7ea
# Regenerate: python -m ci.workflows --write

name: Schedule product-qualification qualification

on:
  schedule:
    - cron: '15 6 * * 6'

# Preserve the token scopes already declared by the called jobs.
permissions:
  actions: read
  contents: read

jobs:
  run:
    uses: ./.github/workflows/product-qualification.yaml
    with:
      product_profile: 'product-extended'
    secrets: inherit
