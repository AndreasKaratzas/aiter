# Generated by ci.workflows; edit the canonical source, not this file.
# Source: .github/workflow-sources/schedules/frameworks/common/canaries.yaml
# Directory guide: .github/workflow-sources/schedules/frameworks/common/README.md
# Source SHA256: b72d72d891c113dd1f7273e86acfb41892354e1ff4813718837e8df49ecc2e4d
# Regenerate: python -m ci.workflows --write

name: Schedule framework compatibility canaries

on:
  schedule:
    - cron: '15 17 * * *'

# Preserve the token scopes already declared by the called jobs.
permissions:
  actions: read
  contents: read

jobs:
  run:
    uses: ./.github/workflows/frameworks-canaries.yaml
    secrets: inherit
