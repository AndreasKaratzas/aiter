# Generated by ci.workflows; edit the canonical source, not this file.
# Source: ci/workflows/schedules/clients/vllm/benchmarks-weekly.yaml
# Source SHA256: c53bf171e2479a6a5bb6a500b40136c871a6efd25238ac499654edb9e026bd82
# Regenerate: python -m ci.workflows --write

name: Schedule client-vllm-model-benchmarks weekly

on:
  schedule:
    - cron: '15 22 * * 0'

# Preserve the token scopes already declared by the called jobs.
permissions:
  actions: read
  contents: read

jobs:
  run:
    uses: ./.github/workflows/client-vllm-model-benchmarks.yaml
    with:
      benchmark_profile: 'extended'
      gpus: '0,1'
      benchmark_cases: ''
    secrets: inherit
