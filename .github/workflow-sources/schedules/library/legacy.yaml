name: Schedule AITER operator tests

on:
  schedule:
    - cron: '0 22 * * *'

# Preserve the token scopes already declared by the called jobs.
permissions:
  contents: read
  id-token: write
  pull-requests: read

jobs:
  run:
    uses: ./.github/workflows/library-legacy.yaml
    secrets: inherit
