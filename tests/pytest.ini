[pytest]
pythonpath = .
addopts = --import-mode=importlib
strict_markers = true
testpaths = unit integration frameworks operators
norecursedirs = .* __pycache__ node_modules

markers =
    l2_device: GPU numerical regression coverage
    rocm_lower: ROCm compiler lowering coverage
